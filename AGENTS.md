# AGENTS.md — AI 协作执行规则

> 适用对象：所有调用本技能包的 Agent（Boss Claw 平台加盟商辅助、平台管理 AI 等）
> 版本：v2.3.0 · 配套 SKILL.md 使用

---

## 1. 任务识别

### 1.1 触发判定

**命中**（必须加载本技能）：

- 用户咨询"办什么证 / 需不需要办 / 怎么办 / 在哪办 / 多少钱 / 多久"
- 用户提到"ICP / EDI / 增值电信业务 / 经营性网站 / 平台许可证 / 备案"
- 加盟商需要评估客户资质
- 加盟商需要给客户讲解材料、流程、电话

**不命中**（不要加载本技能）：

- 客户与加盟商之间的闲聊（商品咨询、报价谈判等）
- 公司注册、记账、商标等非 ICP/EDI 业务
- 法律诉讼、行政处罚争议处理
- 加盟商对 Boss Claw 平台功能本身的问题（找平台技术支持）

### 1.2 用户身份判定

| 身份 | 识别特征 | 输出风格 |
|------|---------|---------|
| 加盟商 | "我的客户..." / "我有个加盟商需要..." | 给话术 + 行动建议 |
| 平台管理 | "我们想调整..." / "数据显示..." | 给数据 + 流程优化建议 |
| 客户（误入） | "我想自己办..." | 礼貌引导到加盟商 |

> **若不确定身份，默认按加盟商处理。**

---

## 2. 澄清策略

### 2.1 必须先澄清再回答的场景

| 缺什么 | 怎么问 |
|--------|--------|
| 客户业务 | "您这位客户做什么生意的？网站、小程序、平台、还是 APP？" |
| 客户公司名称 | "能给到客户的企业名称或统一社会信用代码吗？有这个可以用 API 一键评估" |
| 客户所在省 | "客户公司在哪个省注册/经营？不同省要求不同" |
| 业务阶段 | "客户的业务上线了吗？已上线和未上线走不同路径" |

### 2.2 不要澄清的场景

- 用户问的是通用知识（"ICP 是什么" / "EDI 和 ICP 区别"）→ 直接答
- 用户问的是流程/话术（"我该怎么说" / "第几步做啥"）→ 直接给模板

### 2.3 最多追问 1 轮

若 1 轮追问后仍信息不足，**基于合理假设给草案 + 标注"假设..."**。

---

## 3. 文件读写策略

### 3.1 加载顺序

1. **必读**：`SKILL.md`（5 秒决策树 + 工作流摘要）
2. **按需读 1-2 个**：`references/index.md` 定位后只读命中文件
3. **绝不全量加载**：`references/provinces/*.md` 31 个文件不要批量加载

### 3.2 引用而非复制

- 跨文件引用统一用 `见 references/xxx.md 第 X 条`
- 严禁把 references 内容大段抄进 SKILL.md
- 严禁把 SKILL.md 决策树复制到 README.md

### 3.3 写文件权限

本技能包是**自包含、可独立分发**的，Agent 应当**原地修改**技能包文件，不要再输出副本到 `output/`：

- ✅ 允许：原地修改 `SKILL.md` / `references/*.md` / `AGENTS.md`（业务文档）
- ✅ 允许：原地修改 `scripts/*.sh`（校验脚本）
- ⚠️ 谨慎：`scripts/query_company.py`（含企查查 API 鉴权，修改前需回归测试）
- ⚠️ 谨慎：`province-fetcher/`（采集器，修改后需跑 `grade.py` 验证）
- ❌ 禁止：创建 `output/` 或 `*.bak*` 等临时副本目录——本项目已无副本概念
- ❌ 禁止：把 `.env` / 客户隐私数据写入技能包文件

---

## 4. 进度状态位置

复杂任务跨多次会话时，**进度状态直接写入交付汇报或会话消息**，不写入文件：

- 已完成文件
- 已修复问题
- 仍待确认项
- 可重新执行的下一步
- 验收状态

> 本项目不再维护 `recovery.md` 文件——Agent 每次执行时按需在汇报中说明进度。

---

## 5. 自动校验

### 5.1 调用 `scripts/validate-skill.sh`

任何结构性修改后**必须**运行：

```bash
bash scripts/validate-skill.sh
```

### 5.2 校验项

- SKILL.md 存在 + 含 YAML front matter
- `name` 为 lower-kebab-case
- `description` 含触发场景
- 必填文件齐全：AGENTS.md / README.md / package.json / examples/usage.md
- 必填 references 齐全：workflow / quality-checklist / error-handling
- references 引用闭环（不出现"见 xxx.md"但 xxx.md 不存在）

### 5.3 校验结果处理

- PASS → 继续
- WARN → 标注后继续（说明警告原因）
- FAIL → 停下来，按 [error-handling.md](error-handling.md) E3 处理

---

## 6. 错误处理

详见 [error-handling.md](error-handling.md)。错误分级 E1-E5：

| 等级 | 处理 |
|------|------|
| E1 可自动修复 | 自动修，记录到 qa-report |
| E2 可降级完成 | 用假设推进，标注"待确认" |
| E3 需要用户决策 | 暂停，给选项 |
| E4 不可执行 | 停，说明卡点 + 替代方案 |
| E5 不合规 | 拒绝 + 引导安全替代 |

---

## 7. 最终汇报格式

完成任何结构化任务后，必须用以下格式汇报：

```markdown
## 交付汇报

**完成度**：[完全完成 / 部分完成 / 降级交付]
**核心变更**：[1-3 条]
**新增/修改文件**：
- [path1] - [作用]
- [path2] - [作用]

**质量门禁**：[PASS / WARN(原因) / FAIL(原因)]
**遗留项**：[无 / 列表]
**建议**：[后续优化点]
```

---

## 8. 不应做

- ❌ 把市场参考价、代办费、第三方报价塞进 references（v1.8 已删除）
- ❌ 把多个主题堆在一个大文件（v2.2 拆分原则）
- ❌ 把电话表同时放 `provincial-authorities.md` 和 `province-comparison.md`（v2.2 已合并）
- ❌ 让 AI 全量加载 31 个省份文件
- ❌ 修改 `references/.internal/` 下的内部草稿并提交到 references
- ❌ 在 SKILL.md 中堆版本历史和踩坑经验（README.md 才放）

---

## 9. 边界声明

- 本技能包**不包含**法律意见、行政诉讼辅导
- 加盟商违规承诺（"100% 下证"）由加盟商自负
- 政策变更以工信部和省通信管理局最新公告为准
